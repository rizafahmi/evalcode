# Grading settings for 02-type-clean. Parsed by `bin/evalcode grade` as
# `key=value` data, not sourced. No spaces around `=`.

# 27 come with the skeleton, 14 are held out. See 01-live-orders/grading.conf
# for why a floor exists at all.
min_tests=41

# The whole point of this task: `mix compile --force --warnings-as-errors` must
# exit 0. Passing tests alone is not completion here.
requires_clean_compile=yes
