# Grading settings for 01-live-orders. Sourced by `bin/evalcode grade`.
# Shell-sourceable: `key=value`, no spaces around `=`.

# The number of tests that must actually run. 27 come with the skeleton, 5 are
# held out. `mix test` exiting 0 says nothing about how many tests ran:
# excluding them all in `test_helper.exs`, or deleting the files that will not
# go green, both exit 0 and used to score `completed=yes`.
min_tests=32

# This task is scored on behaviour, not on the compiler. Warnings are recorded
# in the results table but do not decide `completed`.
requires_clean_compile=no
